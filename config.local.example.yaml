# Local AdLoop configuration used by the helper scripts in this fork.
# Copy to .local/config.yaml and fill in the blank values.

google:
  project_id: ""
  credentials_path: ".local/credentials.json"
  token_path: ".local/token.json"

ga4:
  property_id: ""

ads:
  developer_token: ""
  customer_id: ""
  login_customer_id: ""

safety:
  max_daily_budget: 50.00
  max_bid_increase_pct: 100
  require_dry_run: true
  log_file: ".local/audit.log"
  blocked_operations: []
